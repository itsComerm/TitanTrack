<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/blanco"
    android:padding="24dp"
    tools:context=".activities.RegisterActivity">

    <!-- Logo -->
    <ImageView
        android:id="@+id/imgLogo"
        android:layout_width="60dp"
        android:layout_height="100dp"
        android:layout_marginTop="32dp"
        android:contentDescription="@string/app_name"
        android:src="@drawable/logo_titantrack"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Título -->
    <TextView
        android:id="@+id/txtTitulo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:fontFamily="@font/sora_bold"
        android:text="@string/registro_titulo"
        android:textColor="@color/gris_oscuro"
        android:textSize="22sp"
        android:textStyle="bold"
        app:layout_constraintTop_toBottomOf="@id/imgLogo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Campo nombre -->
    <EditText
        android:id="@+id/etNombre"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:autofillHints="name"
        android:background="@drawable/bg_input_field"
        android:fontFamily="@font/dm_sans_medium"
        android:hint="@string/registro_nombre_hint"
        android:inputType="textPersonName"
        android:textColor="@color/gris_oscuro"
        android:textColorHint="@color/gris_medio"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/txtTitulo"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Campo email -->
    <EditText
        android:id="@+id/etEmail"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:autofillHints="emailAddress"
        android:background="@drawable/bg_input_field"
        android:fontFamily="@font/dm_sans_medium"
        android:hint="@string/registro_email_hint"
        android:inputType="textEmailAddress"
        android:textColor="@color/gris_oscuro"
        android:textColorHint="@color/gris_medio"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/etNombre"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Campo contraseña -->
    <EditText
        android:id="@+id/etPassword"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:autofillHints="password"
        android:background="@drawable/bg_input_field"
        android:fontFamily="@font/dm_sans_medium"
        android:hint="@string/registro_password_hint"
        android:inputType="textPassword"
        android:textColor="@color/gris_oscuro"
        android:textColorHint="@color/gris_medio"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/etEmail"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Campo confirmar contraseña -->
    <EditText
        android:id="@+id/etConfirmarPassword"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:autofillHints="password"
        android:background="@drawable/bg_input_field"
        android:fontFamily="@font/dm_sans_medium"
        android:hint="@string/registro_confirmar_password_hint"
        android:inputType="textPassword"
        android:textColor="@color/gris_oscuro"
        android:textColorHint="@color/gris_medio"
        android:textSize="15sp"
        app:layout_constraintTop_toBottomOf="@id/etPassword"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Botón registro -->

    <!-- Enlace a login -->
    <Button
        android:id="@+id/btnRegistro"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:background="@drawable/bg_boton_login"
        android:elevation="0dp"
        android:fontFamily="@font/dm_sans_medium"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:stateListAnimator="@null"
        android:text="@string/registro_boton"
        android:textColor="@color/blanco"
        android:textSize="16sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/etConfirmarPassword" />

    <TextView
        android:id="@+id/txtEnlaceLogin"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:fontFamily="@font/dm_sans_light"
        android:padding="8dp"
        android:text="@string/registro_enlace_login"
        android:textColor="@color/gris_medio"
        app:layout_constraintTop_toBottomOf="@id/btnRegistro"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>